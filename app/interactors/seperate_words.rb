class SeperateWords
	#TO DO:
	#Check for negations and if found include it
	#Check for some adjectives and including it in the word.
	def seperate(words)
		#array = words.split(/[\s,.!?:;]+/)
		return words.split(/[\s,.!?:;]+/)
	end

	def check_negation(words)
		#words.each do |word|
		#	if word.eql? 
	end

	def check_adjective

	end
end