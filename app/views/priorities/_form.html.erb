<%= form_for @prio, html: {class: "form-group"} do |f| %>

  <%= f.label :name %>:
  <%= f.text_field :name,class: "form-control", placeholder: "Name" %><br />

  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>